// Usando while mostrar todos los números de N a 1 disminuyendo de 1 en 1 donde n lo ingresa el usuario en un prompt.

let n = Number(prompt('Ingrese n:'));
let numero = 1;

while(numero <= n) {
    console.log(n);
    n--;
}