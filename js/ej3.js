// Escriba un programa que pdia un numero y diga si es divisible por 2.

let n1 = Number(prompt("Ingrese un numero"));

if(n1 % 2 == 0) {
    console.log(`El numero: ${n1} es divisible por 2`); 
} else {
    console.log(`El numero ${n1} no es divisible por 2`);
    
}

